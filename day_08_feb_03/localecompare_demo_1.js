console.log('ram'.localeCompare('shyam')); // -1 when the first string is smaller than 2nd one
console.log('deepak'.localeCompare('ajit')); // 1 when the first string is smaller than 2nd one
console.log('deepak'.localeCompare('deepak')); // 0 when both the strings are same

// localeCompare() returns 0 , -1 , 1
